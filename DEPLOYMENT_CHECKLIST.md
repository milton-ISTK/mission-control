# 🚀 ISTK Mission Control - Deployment Checklist\n\n**Step-by-step guide to get your dashboard running locally on Mac mini**\n\n---\n\n## Phase 1: Prerequisites (5 min)\n\n### Step 1.1: Verify Node.js\n\n```bash\nnode --version  # Should be v18 or higher\nnpm --version   # Should be v10 or higher\n```\n\n**If missing:** Install from https://nodejs.org (LTS version)\n\n### Step 1.2: Verify OpenClaw CLI\n\n```bash\nopenclaw --version\nopenclaw cron list  # Should list any existing cron jobs\n```\n\n**If error:** OpenClaw not installed. Install from https://openclaw.ai\n\n---\n\n## Phase 2: Convex Setup (10 min)\n\n### Step 2.1: Create Convex Account\n\n```bash\ncd /Users/milton/.openclaw/workspace/mission-control\nnpx convex auth\n```\n\nThis will:\n1. Open https://dashboard.convex.dev in your browser\n2. Ask you to sign up (email + password)\n3. Return a **Deployment URL** to your terminal\n\n### Step 2.2: Copy Deployment URL\n\nYou'll see something like:\n```\nDeployment URL: https://your-name-xyz.convex.cloud\n```\n\n**Save this URL** — you'll need it next.\n\n### Step 2.3: Create .env.local\n\n```bash\ncat > /Users/milton/.openclaw/workspace/mission-control/.env.local << 'EOF'\nNEXT_PUBLIC_CONVEX_URL=https://your-name-xyz.convex.cloud\nEOF\n```\n\n**Replace `https://your-name-xyz.convex.cloud` with YOUR deployment URL from Step 2.2.**\n\nVerify the file was created:\n```bash\ncat /Users/milton/.openclaw/workspace/mission-control/.env.local\n```\n\n---\n\n## Phase 3: Install Dependencies (3 min)\n\n```bash\ncd /Users/milton/.openclaw/workspace/mission-control\nnpm install\n```\n\nThis installs:\n- NextJS 14\n- React 18\n- Convex client\n- Tailwind CSS\n- Icons (lucide-react)\n- Date utilities (date-fns)\n- Markdown renderer (react-markdown)\n- Drag-drop library (@dnd-kit)\n\n**Status:** You should see `added X packages` at the end.\n\n---\n\n## Phase 4: Start the Backend (Terminal 1)\n\n**Important:** This runs in the background. Keep this terminal open.\n\n```bash\ncd /Users/milton/.openclaw/workspace/mission-control\nnpx convex dev\n```\n\nYou should see:\n```\n✅ Database synced!\n✅ Listening for file changes...\n⏳ Ready to push schema...\n```\n\n**Do not close this terminal.** Leave it running.\n\n---\n\n## Phase 5: Start the Frontend (Terminal 2)\n\n**Open a NEW terminal window** (⌘T in Mac terminal)\n\n```bash\ncd /Users/milton/.openclaw/workspace/mission-control\nnpm run dev\n```\n\nYou should see:\n```\n▲ Next.js 14.x.x\n- Local:        http://localhost:3000\n- Environments: .env.local\n✓ Ready in XXXms\n```\n\n---\n\n## Phase 6: Access the Dashboard\n\n1. **Open your browser** and go to: http://localhost:3000\n2. **You should see:**\n   - ISTK logo + \"Agentic Mission Control\" title\n   - Dashboard with quick stats\n   - Sidebar navigation (Tasks, Calendar, Memories, Team, Subagents)\n   - Dark theme with orange accents\n\n**If you see a blank screen:**\n- Check browser console (F12 → Console tab)\n- Look for errors\n- Verify `.env.local` was created correctly\n- Restart both servers (Ctrl+C in each terminal, then re-run)\n\n---\n\n## Phase 7: Verify Features Work\n\n### 7.1: Memory Sync\n1. Go to **Memories** tab\n2. You should see your MEMORY.md indexed automatically\n3. Try searching for a keyword\n\n**If no memories appear:**\n- Check that `/Users/milton/.openclaw/workspace/MEMORY.md` exists\n- Refresh the browser\n- Check `/api/memory-sync` manually: `curl http://localhost:3000/api/memory-sync`\n\n### 7.2: Cron Sync\n1. Go to **Calendar** tab\n2. You should see any active cron jobs\n\n**If no events appear:**\n- Run `openclaw cron list` in a terminal to see if jobs exist\n- Refresh browser\n- Check `/api/cron` manually: `curl http://localhost:3000/api/cron`\n\n### 7.3: Create a Task\n1. Go to **Tasks** tab\n2. Click \"Add Task\" button\n3. Fill in title, assignee (Gregory/Milton), priority\n4. Click \"Create Task\"\n5. Task should appear in \"To Do\" column\n\n### 7.4: Search Memories\n1. Go to **Memories** tab\n2. Type in the search box\n3. Results should filter in real-time\n\n---\n\n## Troubleshooting\n\n### \"Convex deployment URL is missing\"\n**Solution:** Create `.env.local` with `NEXT_PUBLIC_CONVEX_URL` (see Phase 2.3)\n\n### \"Cannot find module @/convex/_generated/api\"\n**Solution:** Run `npx convex dev` first — it auto-generates types when schema syncs\n\n### \"Port 3000 is already in use\"\n**Solution:** Kill the process or use a different port:\n```bash\nnpm run dev -- -p 3001  # Use port 3001 instead\n```\n\n### \"Memories not appearing\"\n**Solution:** \n1. Verify MEMORY.md exists: `ls /Users/milton/.openclaw/workspace/MEMORY.md`\n2. Check file sync endpoint: `curl http://localhost:3000/api/memory-sync | jq`\n3. Refresh browser (Cmd+Shift+R)\n4. Restart Convex backend (Ctrl+C, then `npx convex dev` again)\n\n### \"Calendar events not showing\"\n**Solution:**\n1. Verify cron jobs exist: `openclaw cron list`\n2. Check cron API endpoint: `curl http://localhost:3000/api/cron | jq`\n3. Refresh browser\n\n### \"Drag-drop not working\"\n**Solution:** This is a known issue with certain keyboard layouts. Try:\n1. Disable caps lock\n2. Use arrow keys instead of dragging\n3. Try a different mouse/trackpad\n\n### \"Build fails with TypeScript errors\"\n**Solution:**\n```bash\nnpm install --save-dev typescript@latest\nnpm run build\n```\n\n---\n\n## Going Further\n\n### Deploy to the Internet (Optional)\n\nTo access from your Windows PC remotely:\n\n1. **Option A: SSH tunnel (simplest)**\n   ```bash\n   ssh -N -L 3000:localhost:3000 milton@192.168.0.63\n   ```\n   Then visit http://localhost:3000 from Windows\n\n2. **Option B: ngrok (easiest public access)**\n   ```bash\n   npx ngrok http 3000\n   ```\n   This creates a public URL like `https://xyz.ngrok.io`\n\n3. **Option C: Vercel (production-grade)**\n   ```bash\n   npm install -g vercel\n   vercel\n   ```\n   Vercel will host it + give you a public URL\n\n### Add More Features\n\nSee README.md for:\n- Adding new pages\n- Creating components\n- Integrating Convex mutations\n- Styling with neumorphic design system\n\n---\n\n## Support\n\nIf you get stuck:\n1. Check browser console (F12)\n2. Check server logs (Terminal 1 & 2)\n3. Run `openclaw doctor` for OpenClaw health check\n4. Email: gregory@intellistake.ai\n\n---\n\n**You're ready! Welcome to Mission Control.** 🚀\n"
